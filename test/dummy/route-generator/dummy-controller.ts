import { http, internal } from "../../../src/core"

export class ProductController{
        
        getByPage(offset:number, pageWidth = 50){}

        get(id:number){}

        add(model){}

        modify(id, model){}

        delete(id:number){}
}